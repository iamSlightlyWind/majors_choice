name: Run Neofetch

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Run Neofetch
      run: |
        sudo apt-get update
        sudo apt-get install -y neofetch
        neofetch