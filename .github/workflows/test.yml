name: Run Tomcat and Keep Workflow Running

on: [push, pull_request]

jobs:
  run-tomcat:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v1

    - name: Run Tomcat container
      run: |
        docker pull tomcat:latest
        docker run -d --name tomcat -p 8080:8080 tomcat:latest

    - name: Install ngrok
      run: |
        curl -s https://ngrok-agent.s3.amazonaws.com/ngrok.asc \
        | sudo tee /etc/apt/trusted.gpg.d/ngrok.asc >/dev/null \
        && echo "deb https://ngrok-agent.s3.amazonaws.com buster main" \
        | sudo tee /etc/apt/sources.list.d/ngrok.list \
        && sudo apt update \
        && sudo apt install ngrok
        ngrok config add-authtoken ${{ secrets.NGROK_AUTHTOKEN }}

    - name: Start ngrok ssh
      id: ngrok
      uses: tmshkr/ngrok-ssh@latest
      with:
        NGROK_AUTHTOKEN: ${{ secrets.NGROK_AUTHTOKEN }}

    - name: Start ngrok tomcat
      run: ngrok http --region=ap http://localhost:8080 &

    - name: Use SSH port
      run: echo "The public port of the SSH server is ${{ steps.ngrok.outputs.SSH_PORT }}"

    - name: Keep the workflow running
      run: |
        echo "Entering infinite loop to keep the workflow running..."
        while true; do sleep 1000; done